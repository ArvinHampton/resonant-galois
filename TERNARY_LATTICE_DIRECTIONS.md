# Next research directions — ternary lattice of resonant algebra

_Necessity remains paused. The live question is: what structure does the lattice itself carry?_

---

## Where we stand

The ternary lattice \(L_0\) (order-3 data + model integers \(\{3,9,27,61,80,243,539,4880,\ldots\}\)) is no longer only a seed bag. It now sits in **three concrete roles**:

| Role | What \(L_0\) does | Evidence |
|------|-------------------|----------|
| **Specialisation source** | Feeds pure-even multi-\(k\) and the B-embed \(A\mapsto P_A\) | `PURE_EVEN_MULTI_K.md`, `B_EMBED_LATTICE.md` |
| **Mestre parameter** | Lattice \(t\) on flagship \(P_t\) stays \(A_5\) | `MESTRE_FLAGSHIP_PT.md` (\(t\in\{3,9,61,80,\ldots\}\)) |
| **Template coordinates** | Realises both BJ-embed and beyond-BJ B-avatar (\(d=-75\)) | `EVENNESS_AVATAR.md` |

**Citable centre** remains pure-even multi-\(k\) + four-face organising principle. Necessity stays **paused**.

---

## Directions (ranked)

### Direction 1 — Secondary invariants on \(L_0\) (**default next**)

**Goal.** Classify lattice specialisations by more than \(k=\beta/\alpha\).

| Invariant | Why it matters |
|-----------|----------------|
| 3-adic valuation \(v_3(\alpha),v_3(\beta),v_3(A)\) | Ternary height of the coefficient |
| Disc prime factors (esp. 3, 5, 61) | Ramification fingerprint |
| Height \(\log\max(\lvert\alpha\rvert,\lvert\beta\rvert)\) | Stage D asymptotics refined by lattice stratum |
| \(A_5\) vs \(D_5\) rate within fixed \(k\) or fixed \(v_3\) | Predict monodromy refinement |
| Which lattice points lie on both PE-rays and B-embed | Overlap geometry of the two evenness avatars |

**Deliverable.** Table/map: lattice point \(\to (k,\,v_3,\,\mathrm{disc\ primes},\,\mathrm{Gal})\).  
**Status:** **Executed** — `L0_SECONDARY_INVARIANTS.md` / `l0_secondary_invariants.py`.

| Slice | n | Gal (checked) | Notes |
|-------|--:|---------------|-------|
| PE multi-\(k\) | 148 | A5=36, D5=4 | \(D_5\) appears — monodromy refinement signal |
| B-embed \(A\in\pm L_0\) | 316 | A5=45, red=2 | disc□ by identity |
| Mestre flagship \(t\in L_0\) | 42 | A5=18 (+even unchecked) | lattice \(t\) stable for \(A_5\) samples |
| Strict PE↔B overlap | 14 | — | \(A=\pm\alpha\) or \(\pm\beta\); no \(\Phi\) yet |

**Why first.** Purely arithmetic, citable, extends Stage D, needs no necessity claim.

---

### Direction 2 — Lattice as monoid / saturation

**Goal.** Treat \(L_0\) as an algebraic object, not a list.

- Resonant monoid generated by \(\{3,61,80,243,539\}\) under multiplication and selected additive short sums.
- Saturation: large 3-smooth integers hit by PE or B-embed? Residue classes mod \(3^m\) for \(k\) or \(A\)?
- Compare monoid to \(\{A:P_A\text{ irr / Gal}=A_5\}\).

**Status:** **Executed** — `L0_MONOID_SATURATION.md` / `l0_monoid_pe_b.py`.

| Object | Size (≤30k) | Note |
|--------|------------:|------|
| \(M_0=\langle 3,61,80,243,539\rangle_\times\) | 32 | = resonant-smooth products |
| \(M_2\) (+ short sums) | 4504 | catalogue-style enrichment |
| B irr rate on monoid sample | ~0.99 | disc□ free; irr residual |
| PE \(\lvert\alpha\rvert\in M_2\) | 19/42 | monoid organises params more than raw coeffs |

---

### Direction 3 — Unify the two evenness avatars on \(L_0\)

| Avatar | Parameter | Beyond BJ? |
|--------|-----------|:----------:|
| PE | \(k=\beta/\alpha\), pure-even ray | No |
| B | \(A\), disc \(=324 A^2(A^2+84375)^2\) | Yes (\(d\neq 0\)) |

**Goal.** Lattice points/paths meaningful in both pictures, or a map
\[
\Phi:\{\text{PE data on }L_0\}\longleftrightarrow\{\text{B-embed data on }L_0\}
\]
preserving Gal \(=A_5\) when possible.

**Status:** **First cut** — `L0_PE_B_UNIFY.md`. No canonical rate-1 \(\Phi\); best elementary maps: \(A=k_{\mathrm{num}}k_{\mathrm{den}}\), \(A=\pm\alpha\) (partial A5 yield, even rate ~1 when defined).

---

### Direction 4 — Mestre orbit of the lattice

- Mestre on every multi-seed PE class and every B-embed \(P_A\) for \(A\in L_0\).
- Lattice stability under Mestre; graph seed → family → \(L_0\) specialisations.

**Status:** **Executed** — `L0_MESTRE_ORBIT.md` / `l0_mestre_orbit.py`.

| Metric | Value |
|--------|------:|
| Seeds (PE + B) | 24 |
| With Mestre \(R\)-space | 24 |
| Families disc□ in \(\mathbb{Q}(t)\) | 24 |
| Even \((\\mathrm{seed},t\in L_0)\) pairs | 480/480 |
| Sample \(A_5\) specs | 35 |
| Graph nodes / edges | 528 / 504 |
| Second Mestre hops (disc□) | 3 |

Stability is at the **parameter** \(t\in L_0\) level (not raw coefficients in the monoid).

---

### Direction 5 — Necessity-facing only (optional)

New matrix avatar with built-in evenness named by HQCC axioms, or Mestre generic fibre axiom-named. **Lower priority** until 1–3 written up.

---

## Recommended sequence

```
1. Secondary invariants on L₀          ← done
2. Resonant monoid / saturation        ← done
3. Unify PE ↔ B avatars on L₀          ← first cut
4. Mestre orbit graph of lattice seeds ← done
5. (Optional) necessity-facing avatar  ← paused
```

## One-line aim

Make the ternary lattice the **theorem-bearing object**: invariants, monoid structure, and simultaneous specialisation in both evenness avatars — without reopening Crit-2 forcing or unpausing necessity.

## Do not reopen

| Settled | Why |
|---------|-----|
| Pure-even multi-\(k\) identity | Finished centre |
| Crit 2 forcing on unrestricted \(T\) | Closed negative |
| Necessity as deliverable | Paused |
| More linear cuts of same \(T\) | Exhausted |

---

_See also: `RESEARCH_ROADMAP.md`, `PURE_EVEN_MULTI_K.md`, `TERNARY_ORGANIZING_PRINCIPLE.md`._
