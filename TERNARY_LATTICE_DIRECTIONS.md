# Ternary lattice L₀ — research directions

## Three roles of L₀ (locked)

| Role | What L₀ does |
|------|----------------|
| Specialisation source | PE multi-k + B-embed A ↦ P_A |
| Mestre parameter | Lattice t on flagship P_t |
| Template coordinates | BJ-embed + beyond-BJ B-avatar (d = −75) |

Necessity remains paused. Live question: what structure does the lattice itself carry?

---

## Direction status

| Dir | Goal | Status |
|-----|------|--------|
| **1** Secondary invariants | Map → (k, v₃, disc primes, height, Gal) | **Done** (`L0_SECONDARY_INVARIANTS.md`) |
| **2** Resonant monoid / saturation | L₀ as algebraic object; densities, residues | Next default |
| **3** Unify PE ↔ B on L₀ | Map or common specialisations | Waits on Dir 1 map; thin overlap (14 pts) noted |
| **4** Mestre orbit of lattice | Stability of L₀ under Mestre | Optional after 2–3 |
| **5** Necessity-facing avatar | Only with built-in evenness identity | Optional / paused |

---

## Recommended sequence (unchanged)

```
1. Secondary invariants on L₀     ← done
2. Resonant monoid / saturation   ← default next
3. Unify PE ↔ B avatars on L₀
4. Mestre orbit graph of lattice seeds
5. (Optional) necessity-facing avatar
```

## Slogan

> Make the ternary lattice the theorem-bearing object: invariants, monoid structure, simultaneous specialisation in both evenness avatars — without reopening Crit-2 forcing or unpausing necessity.
